name: typesql_parser
description: SQL parser, AST and visitors
version: 0.0.1
repository: https://github.com/juancastillo0/wasm_run
topics:
  - wasm
  - wit
  - sql
  - parser
  - ast

environment:
  sdk: ^3.10.0
resolution: workspace

flutter:
  assets:
    - lib/assets/

dependencies:
  wasm_run: ^0.1.0
  wasm_wit_component: ^0.0.1

dev_dependencies:
  lints: ^6.1.0
  test: ^1.30.0
