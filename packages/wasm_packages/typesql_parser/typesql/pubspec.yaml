name: typesql
description: SQL type utilities and helpers for Dart model code generator and query execution
version: 0.2.0
repository: https://github.com/juancastillo0/wasm_run
topics:
  - sql

environment:
  sdk: ^3.10.0
resolution: workspace

dependencies:
  typesql_parser: ^0.2.0
  meta: ^1.17.0
  sqlite3: ^3.3.0
  recase: ^4.1.0
  wasm_wit_component: ^0.2.0

dev_dependencies:
  lints: ^6.1.0
  test: ^1.30.0
