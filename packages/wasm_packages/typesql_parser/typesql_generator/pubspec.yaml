name: typesql_generator
description: SQL types code generator from .sql files to Dart queries and models
version: 0.2.0
repository: https://github.com/juancastillo0/wasm_run
topics:
  - sql
  - codegen

environment:
  sdk: ^3.10.0
resolution: workspace

dependencies:
  typesql: ^0.2.0
  build: ^4.0.5
  recase: ^4.1.0
  dart_style: ^3.1.7

dev_dependencies:
  build_runner: 2.13.1
  build_config: 1.3.0
  lints: ^6.1.0
  test: ^1.30.0
