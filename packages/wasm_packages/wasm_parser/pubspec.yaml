name: wasm_parser
description: Parses Web Assembly (WASM), WASM Text Format (WAT) and Web Assembly Interface Types (WIT) files
version: 0.2.0
repository: https://github.com/juancastillo0/wasm_run
topics:
  - wasm
  - wit
  - parser
  - wat

environment:
  sdk: ^3.10.0
resolution: workspace

flutter:
  assets:
    - lib/assets/

dependencies:
  wasm_run: ^0.2.0
  wasm_wit_component: ^0.2.0

dev_dependencies:
  lints: ^6.1.0
  test: ^1.30.0
