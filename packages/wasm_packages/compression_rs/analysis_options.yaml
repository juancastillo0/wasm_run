include: package:lints/recommended.yaml

analyzer:
  language:
    strict-casts: true
    strict-inference: true
    strict-raw-types: true

  # You might want to exclude generated files from dart analysis
  exclude:
    # - "lib/src/compression_rs_wit.gen.dart"
