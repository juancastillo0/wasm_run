[package]
name = "rust_threads_example"
version = "0.1.0"
# Uses nightly features (portable_simd), keep on 2021
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html
[lib]
crate-type = ['cdylib']

[dependencies]
